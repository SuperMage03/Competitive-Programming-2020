#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);


    int daysLeft, leastN;
    cin >> daysLeft;
    leastN = (int)ceil(log2l(daysLeft)) + 1;

    for (int i = 0; i < leastN; i++) {
        for (int j = 1)
    }

    return 0;
}
