#include <bits/stdc++.h>
using namespace std;

int main() {
    cin.sync_with_stdio(false);
    cin.tie(nullptr);

    int cities;
    cin >> cities;

    int people[cities + 1];
    for (int i = 1; i < cities + 1; i++) {
        cin >> people[i];
    }


    return 0;
}
